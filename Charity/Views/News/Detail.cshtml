@model Charity.Models.News
@{
    ViewBag.Title = Model.NewsName;
}
<div class="container-fluid page-header mb-5 wow fadeIn" data-wow-delay="0.1s">
    <div class="container text-center">
        <h2 class="display-4 text-white animated slideInDown mb-4">@Model.NewsName</h2>
    </div>
</div>
<div class="text-center"><img src="/Content/img/@Model.NewsImage" class="img-fluid" ></div>
<div class="container mt-5 mb-5 text-dark">
    @Html.Raw(Model.Descrpition)
</div>
